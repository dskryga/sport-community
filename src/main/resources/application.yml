spring:
  application:
    name: spoc

  liquibase:
    enabled: true

  datasource:
    url: jdbc:postgresql://localhost:5433/community
    username: dbuser
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: redis

app:
  jwt:
    secret: jTexB946VXnIS3diqLDQin52T1bCy8Tj
    expiration-ms: 86400000 # 24 hours

logging:
  level:
    ru.skriagin: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE